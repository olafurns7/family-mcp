# Krónan MCP plumbing report

## Files changed

- `.github/workflows/release.yml`
- `.oxfmtrc.json`
- `.oxlintrc.json`
- `CLAUDE.md`
- `README.md`
- `bun.lock`
- `docs/AGENTS.md`
- `docs/analysis/kronan-mcp-plumbing-report.md`
- `package.json`
- `packages/abler-mcp/README.md` (generated by the completed Abler part of `release:sync`)
- `packages/infomentor-mcp/docs/RELEASING.md` (generated by the completed InfoMentor part of `release:sync`)
- `packages/kronan-mcp/api/kronan-api.d.ts`
- `packages/kronan-mcp/package.json`
- `packages/kronan-mcp/test/api-types.test.ts`
- `packages/kronan-mcp/tsconfig.json`
- `packages/session-store/README.md`
- `tooling/release/mcp-smoke.mjs`
- `tooling/release/package.mjs`
- `tooling/release/release.test.mjs`
- `tooling/release/sync-root-readme.mjs`
- `tooling/release/sync-version.mjs`

The generated declaration and the type-only test are the only changes below the
new package's API/test area. No file under `packages/kronan-mcp/src/` was edited.

## Commands and results

| Command | Result |
| --- | --- |
| `bun --version` | Passed: `1.4.2`. |
| `bun install` | Passed and updated `bun.lock`. `openapi-typescript@7.13.0` requires TypeScript `^5.x`; the package therefore declares its exact local `typescript@5.9.3` dev peer while the root remains on TypeScript 7.0.2. |
| `bun install --frozen-lockfile` | Passed: 80 installs across 146 packages, with no changes. |
| `bunx --bun openapi-typescript api/openapi.json -o api/kronan-api.d.ts` | Passed; generated `api/kronan-api.d.ts`. The `--bun` runner resolves the package-local TypeScript 5 peer. |
| `bun run api:check` | Passed. |
| `bun run typecheck` | Passed. |
| `bun test test/api-types.test.ts --timeout 30000` | Passed: 1 test, 0 failures; no network access. |
| `bunx turbo run typecheck lint format:check --filter=kronan-mcp --force` | Passed: 7 tasks. |
| `bunx turbo run lint format:check --force` | Passed: 15 tasks. The vendored `api/openapi.json` and generated `api/kronan-api.d.ts` are excluded from lint/format so the vendored source remains unchanged. |
| `bun run release:sync` | Partially completed: root README, Abler, and InfoMentor synchronization ran; Krónan stopped with `ENOENT` for the protected, currently absent `packages/kronan-mcp/README.md`. No Krónan `install.sh` was generated. |
| `bunx turbo run test --filter=@family-mcp/release-tooling --force` | Release generation subtest passed. The cache-key and synchronization subtests failed because their deliberate `git ls-files` scratch copies exclude the still-untracked `packages/kronan-mcp`, so `kronan-mcp` task hashes are absent and its scratch `package.json` is missing. |

## OpenAPI/Zod compatibility

The generated type test checks all listed compatible response schemas and the
`searchRecipesInput` and `lookupProductsInput` request bodies. One requested
request-body check cannot be asserted without changing source:

- `packages/kronan-mcp/src/schemas.ts:55-60` — `searchProductsInput.sortBy` is
  Zod-optional, so its output type permits `sortBy: undefined`. Generated
  `PublicSearchInput` has `sortBy?: string`; with `exactOptionalPropertyTypes`,
  the former is not assignable to the latter. The schema was not loosened or
  otherwise changed.

## Unfinished work

- Luna-owned `packages/kronan-mcp/README.md`, `CHANGELOG.md`, and
  `docs/RELEASING.md` are absent in the shared tree. Their absence prevents
  Krónan `release:sync` from creating `install.sh`.
- The new package is still untracked. Once it is tracked by its owner, rerun
  `bun run release:sync` and
  `bunx turbo run test --filter=@family-mcp/release-tooling --force` to exercise
  the cache-key and race regressions against it.
